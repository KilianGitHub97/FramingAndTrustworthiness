8)
)
l = l + 1
}
}
NegCor <- list()
info_type <- c("PastGame", "Promise", "PersTrait")
measure_type <- c("Trust", "Conf", "Transfer")
PosCor <- list()
NegCor <- list()
l = 1
for (info in 1:3) {
for (mes in 1:3) {
#set up variables
var_one <- paste0(
"data$",
info_type[info],
"1_PosFrame",
measure_type[mes]
)
var_two <- paste0(
"data$",
info_type[info],
"2_PosFrame",
measure_type[mes]
)
var_three <- paste0(
"data$",
info_type[info],
"1_NegFrame",
measure_type[mes]
)
var_four <- paste0(
"data$",
info_type[info],
"2_NegFrame",
measure_type[mes]
)
#Positive Frame: Correlation & Results
cor <- cor.test(
eval(parse(text = var_one)),
eval(parse(text = var_two)),
method = "spearman"
)
PosCor[[l]] <- data.table(
Name = paste(
info_type[info],
"PosFrame",
measure_type[mes],
sep = "_"),
Correlation = round(
cor$estimate,
4),
p.value = round(
cor$p.value,
8)
)
#Negative Frame: Correlation & Results
cor <- cor.test(
eval(parse(text = var_three)),
eval(parse(text = var_four)),
method = "spearman"
)
negCor[[l]] <- data.table(
Name = paste(
info_type[info],
"NegFrame",
measure_type[mes],
sep = "_"),
Correlation = round(
cor$estimate,
4),
p.value = round(
cor$p.value,
8)
)
l = l + 1
}
}
info_type <- c("PastGame", "Promise", "PersTrait")
measure_type <- c("Trust", "Conf", "Transfer")
PosCor <- list()
NegCor <- list()
l = 1
for (info in 1:3) {
for (mes in 1:3) {
#set up variables
var_one <- paste0(
"data$",
info_type[info],
"1_PosFrame",
measure_type[mes]
)
var_two <- paste0(
"data$",
info_type[info],
"2_PosFrame",
measure_type[mes]
)
var_three <- paste0(
"data$",
info_type[info],
"1_NegFrame",
measure_type[mes]
)
var_four <- paste0(
"data$",
info_type[info],
"2_NegFrame",
measure_type[mes]
)
#Positive Frame: Correlation & Results
cor <- cor.test(
eval(parse(text = var_one)),
eval(parse(text = var_two)),
method = "spearman"
)
PosCor[[l]] <- data.table(
Name = paste(
info_type[info],
"PosFrame",
measure_type[mes],
sep = "_"),
Correlation = round(
cor$estimate,
4),
p.value = round(
cor$p.value,
8)
)
#Negative Frame: Correlation & Results
cor <- cor.test(
eval(parse(text = var_three)),
eval(parse(text = var_four)),
method = "spearman"
)
NegCor[[l]] <- data.table(
Name = paste(
info_type[info],
"NegFrame",
measure_type[mes],
sep = "_"),
Correlation = round(
cor$estimate,
4),
p.value = round(
cor$p.value,
8)
)
l = l + 1
}
}
warnings()
rbind(PosCor, NegCor)
cor_table <- rbindlist(rbind(PosCor, NegCor)
)
View(cor_table)
info_type <- c("PastGame", "Promise", "PersTrait")
measure_type <- c("Trust", "Conf", "Transfer")
PosCor <- list()
NegCor <- list()
l = 1
for (info in 1:3) {
for (mes in 1:3) {
#set up variables
var_one <- paste0(
"data$",
info_type[info],
"1_PosFrame",
measure_type[mes]
)
var_two <- paste0(
"data$",
info_type[info],
"2_PosFrame",
measure_type[mes]
)
var_three <- paste0(
"data$",
info_type[info],
"1_NegFrame",
measure_type[mes]
)
var_four <- paste0(
"data$",
info_type[info],
"2_NegFrame",
measure_type[mes]
)
#Positive Frame: Correlation & Results
cor <- cor.test(
eval(parse(text = var_one)),
eval(parse(text = var_two)),
method = "pearson"
)
PosCor[[l]] <- data.table(
Name = paste(
info_type[info],
"PosFrame",
measure_type[mes],
sep = "_"),
Correlation = round(
cor$estimate,
4),
p.value = round(
cor$p.value,
8)
)
#Negative Frame: Correlation & Results
cor <- cor.test(
eval(parse(text = var_three)),
eval(parse(text = var_four)),
method = "pearson"
)
NegCor[[l]] <- data.table(
Name = paste(
info_type[info],
"NegFrame",
measure_type[mes],
sep = "_"),
Correlation = round(
cor$estimate,
4),
p.value = round(
cor$p.value,
8)
)
l = l + 1
}
}
cor_table <- rbindlist(
rbind(PosCor, NegCor)
)
infos <- c("PastGame", "Promise", "PersTrait")
measures <- c("Trust", "Conf", "Transfer")
conditions <- c("_PosFrame", "_NegFrame")
cor_table <- list()
l = 1
for (info in 1:3) {
for (mes in 1:3) {
for (cond in 1:2) {
#set up variables
var_one <- paste0(
"data$",
infos[info],
"1",
conditions[cond],
measures[mes]
)
var_one <- paste0(
"data$",
infos[info],
"2",
conditions[cond],
measures[mes]
)
#Correlation & Results
cor <- cor.test(
eval(parse(text = var_one)),
eval(parse(text = var_two)),
method = "spearman"
)
cor_table[[l]] <- data.table(
Name = paste(
infos[info],
conditions[cond],
measures[mes],
sep = "_"),
Correlation = round(
cor$estimate,
4),
p.value = round(
cor$p.value,
8)
)
l = l + 1
}
}
}
cond = 1
#set up variables
var_one <- paste0(
"data$",
infos[info],
"1",
conditions[cond],
measures[mes]
)
var_one
var_one <- paste0(
"data$",
infos[info],
"2",
conditions[cond],
measures[mes]
)
var_one
#Correlation & Results
cor <- cor.test(
eval(parse(text = var_one)),
eval(parse(text = var_two)),
method = "spearman"
)
cor_table[[l]] <- data.table(
Name = paste(
infos[info],
conditions[cond],
measures[mes],
sep = "_"),
Correlation = round(
cor$estimate,
4),
p.value = round(
cor$p.value,
8)
)
l = l + 1
infos <- c("PastGame", "Promise", "PersTrait")
measures <- c("Trust", "Conf", "Transfer")
conditions <- c("_PosFrame", "_NegFrame")
cor_table <- list()
l = 1
for (info in 1:3) {
for (mes in 1:3) {
for (cond in 1:2) {
#set up variables
var_one <- paste0(
"data$",
infos[info],
"1",
conditions[cond],
measures[mes]
)
var_one <- paste0(
"data$",
infos[info],
"2",
conditions[cond],
measures[mes]
)
#Correlation & Results
cor <- cor.test(
eval(parse(text = var_one)),
eval(parse(text = var_two)),
method = "spearman"
)
cor_table[[l]] <- data.table(
Name = paste(
infos[info],
conditions[cond],
measures[mes],
sep = "_"),
Correlation = round(
cor$estimate,
4),
p.value = round(
cor$p.value,
8)
)
l = l + 1
}
}
}
cor_table <- rbindlist(cor_table)
View(cor_table)
#Correlation Table
infos <- c("PastGame", "Promise", "PersTrait")
measures <- c("Trust", "Conf", "Transfer")
conditions <- c("_PosFrame", "_NegFrame")
cor_table <- list()
l = 1
for (info in 1:3) {
for (mes in 1:3) {
for (cond in 1:2) {
#set up variables
var_one <- paste0(
"data$",
infos[info],
"1",
conditions[cond],
measures[mes]
)
var_one <- paste0(
"data$",
infos[info],
"2",
conditions[cond],
measures[mes]
)
#Correlation & Results
cor <- cor.test(
eval(parse(text = var_one)),
eval(parse(text = var_two)),
method = "pearson"
)
cor_table[[l]] <- data.table(
Name = paste(
infos[info],
conditions[cond],
measures[mes],
sep = "_"),
Correlation = round(
cor$estimate,
4),
p.value = round(
cor$p.value,
8)
)
l = l + 1
}
}
}
cor_table <- rbindlist(cor_table)
View(cor_table)
#Correlation Table
infos <- c("PastGame", "Promise", "PersTrait")
measures <- c("Trust", "Conf", "Transfer")
conditions <- c("_PosFrame", "_NegFrame")
cor_table <- list()
l = 1
for (info in 1:3) {
for (mes in 1:3) {
for (cond in 1:2) {
#set up variables
var_one <- paste0(
"data$",
infos[info],
"1",
conditions[cond],
measures[mes]
)
var_two <- paste0(
"data$",
infos[info],
"2",
conditions[cond],
measures[mes]
)
#Correlation & Results
cor <- cor.test(
eval(parse(text = var_one)),
eval(parse(text = var_two)),
method = "spearman"
)
cor_table[[l]] <- data.table(
Name = paste(
infos[info],
conditions[cond],
measures[mes],
sep = "_"),
Correlation = round(
cor$estimate,
4),
p.value = round(
cor$p.value,
8)
)
l = l + 1
}
}
}
cor_table <- rbindlist(cor_table)
View(cor_table)
infos <- c("PastGame", "Promise", "PersTrait")
measures <- c("Trust", "Conf", "Transfer")
conditions <- c("_PosFrame", "_NegFrame")
cor_table <- list()
l = 1
for (info in 1:3) {
for (mes in 1:3) {
for (cond in 1:2) {
#set up variables
var_one <- paste0(
"data$",
infos[info],
"1",
conditions[cond],
measures[mes]
)
var_two <- paste0(
"data$",
infos[info],
"2",
conditions[cond],
measures[mes]
)
#Correlation & Results
cor <- cor.test(
eval(parse(text = var_one)),
eval(parse(text = var_two)),
method = "spearman"
)
cor_table[[l]] <- data.table(
Name = paste(
infos[info],
conditions[cond],
measures[mes],
sep = "_"),
Correlation = round(
cor$estimate,
4),
p.value = round(
cor$p.value,
4)
)
l = l + 1
}
}
}
cor_table <- rbindlist(cor_table)
View(cor_table)
View(cor_table)
