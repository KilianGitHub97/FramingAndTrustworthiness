)
WilcoxTest <- wilcox.test(
na.omit(eval(parse(text = var_one))),
na.omit(eval(parse(text = var_two))),
paired = FALSE,
na.rm = TRUE
)
FramingTest[[l]] <- data.table(
var1 = var_one,
var2 = var_two,
w.stat = WilcoxTest$statistic,
p.value = WilcoxTest$p.value
)
l = l + 1
}
eval(parse(text = var_one))
x = as.character("data$PastGame_PosFrame_Trust")
x
eval(parse(text = x))
source('~/GitHub/FramingAndTrustworthiness/analyses/code/Analysis.R', echo=TRUE)
FramingTest <- list()
l = 1
for(i in 1:3){
var_one <- paste0(
"data$",
infos[i],
conditions[1],
"_",
measures[i]
)
var_two <- paste0(
"data$",
infos[i],
conditions[2],
"_",
measures[i]
)
WilcoxTest <- wilcox.test(
na.omit(eval(parse(text = var_one))),
na.omit(eval(parse(text = var_two))),
paired = FALSE,
na.rm = TRUE
)
FramingTest[[l]] <- data.table(
var1 = var_one,
var2 = var_two,
w.stat = WilcoxTest$statistic,
p.value = WilcoxTest$p.value
)
l = l + 1
}
FramingTest <- data.table()
l = 1
for(i in 1:3){
var_one <- paste0(
"data$",
infos[i],
conditions[1],
"_",
measures[i]
)
var_two <- paste0(
"data$",
infos[i],
conditions[2],
"_",
measures[i]
)
WilcoxTest <- wilcox.test(
na.omit(eval(parse(text = var_one))),
na.omit(eval(parse(text = var_two))),
paired = FALSE,
na.rm = TRUE
)
FramingTest[[l]] <- data.table(
var1 = var_one,
var2 = var_two,
w.stat = WilcoxTest$statistic,
p.value = WilcoxTest$p.value
)
l = l + 1
}
FramingTest <- data.table()
l = 1
for(i in 1:3){
var_one <- paste0(
"data$",
infos[i],
conditions[1],
"_",
measures[i]
)
var_two <- paste0(
"data$",
infos[i],
conditions[2],
"_",
measures[i]
)
WilcoxTest <- wilcox.test(
na.omit(eval(parse(text = var_one))),
na.omit(eval(parse(text = var_two))),
paired = FALSE,
na.rm = TRUE
)
FramingTest[l] <- data.table(
var1 = var_one,
var2 = var_two,
w.stat = WilcoxTest$statistic,
p.value = WilcoxTest$p.value
)
l = l + 1
}
FramingTest
FramingTest <- list()
l = 1
FramingTest <- rbindlist(FramingTest)
FramingTest <- list()
l = 1
for(i in 1:3){
var_one <- paste0(
"data$",
infos[i],
conditions[1],
"_",
measures[i]
)
var_two <- paste0(
"data$",
infos[i],
conditions[2],
"_",
measures[i]
)
WilcoxTest <- wilcox.test(
na.omit(eval(parse(text = var_one))),
na.omit(eval(parse(text = var_two))),
paired = FALSE
)
FramingTest[[l]] <- data.table(
var1 = var_one,
var2 = var_two,
w.stat = WilcoxTest$statistic,
p.value = WilcoxTest$p.value
)
l = l + 1
}
View(FramingTest)
for(i in 1:3){
for (j in 1:3) {
var_one <- paste0(
"data$",
infos[i],
conditions[1],
"_",
measures[j]
)
var_two <- paste0(
"data$",
infos[i],
conditions[2],
"_",
measures[j]
)
WilcoxTest <- wilcox.test(
na.omit(eval(parse(text = var_one))),
na.omit(eval(parse(text = var_two))),
paired = FALSE
)
FramingTest[[l]] <- data.table(
var1 = var_one,
var2 = var_two,
w.stat = WilcoxTest$statistic,
p.value = WilcoxTest$p.value
)
l = l + 1
}
}
View(FramingTest)
FramingTest <- rbindlist(FramingTest)
View(FramingTest)
FramingTest <- list()
l = 1
for(i in 1:3){
for (j in 1:3) {
var_one <- paste0(
"data$",
infos[i],
conditions[1],
"_",
measures[j]
)
var_two <- paste0(
"data$",
infos[i],
conditions[2],
"_",
measures[j]
)
WilcoxTest <- wilcox.test(
na.omit(eval(parse(text = var_one))),
na.omit(eval(parse(text = var_two))),
paired = FALSE
)
FramingTest[[l]] <- data.table(
var1 = var_one,
var2 = var_two,
w.stat = round(WilcoxTest$statistic, 4),
p.value = round(WilcoxTest$p.value, 4)
)
l = l + 1
}
}
FramingTest <- rbindlist(FramingTest)
View(FramingTest)
FramingTest <- p.adjust(FramingTest$p.value)
FramingTest <- list()
l = 1
for(i in 1:3){
for (j in 1:3) {
var_one <- paste0(
"data$",
infos[i],
conditions[1],
"_",
measures[j]
)
var_two <- paste0(
"data$",
infos[i],
conditions[2],
"_",
measures[j]
)
WilcoxTest <- wilcox.test(
na.omit(eval(parse(text = var_one))),
na.omit(eval(parse(text = var_two))),
paired = FALSE
)
FramingTest[[l]] <- data.table(
var1 = var_one,
var2 = var_two,
w.stat = round(WilcoxTest$statistic, 4),
p.value = round(WilcoxTest$p.value, 4)
)
l = l + 1
}
}
FramingTest <- rbindlist(FramingTest)
FramingTest <- p.adjust(FramingTest$p.value)
FramingTest
FramingTest <- list()
l = 1
for(i in 1:3){
for (j in 1:3) {
var_one <- paste0(
"data$",
infos[i],
conditions[1],
"_",
measures[j]
)
var_two <- paste0(
"data$",
infos[i],
conditions[2],
"_",
measures[j]
)
WilcoxTest <- wilcox.test(
na.omit(eval(parse(text = var_one))),
na.omit(eval(parse(text = var_two))),
paired = FALSE
)
FramingTest[[l]] <- data.table(
var1 = var_one,
var2 = var_two,
w.stat = round(WilcoxTest$statistic, 4),
p.value = round(WilcoxTest$p.value, 4)
)
l = l + 1
}
}
FramingTest <- rbindlist(FramingTest)
FramingTest$p.adjust <- p.adjust(FramingTest$p.value)
FramingTest
pkgs <- c(
"tidyverse",
"data.table",
"qgraph",
"effsize"
)
lapply(pkgs, library, character.only = TRUE)
pkgs <- c(
"tidyverse",
"data.table",
"qgraph",
"effsize"
)
lapply(pkgs, library, character.only = TRUE)
FramingTest <- list()
l = 1
for(i in 1:3){
for (j in 1:3) {
var_one <- paste0(
"data$",
infos[i],
conditions[1],
"_",
measures[j]
)
var_two <- paste0(
"data$",
infos[i],
conditions[2],
"_",
measures[j]
)
WilcoxTest <- wilcox.test(
na.omit(eval(parse(text = var_one))),
na.omit(eval(parse(text = var_two))),
paired = FALSE
)
FramingTest[[l]] <- data.table(
var1 = var_one,
var2 = var_two,
w.stat = round(WilcoxTest$statistic, 4),
p.value = round(WilcoxTest$p.value, 4)
cohen.d = cohen.d(
na.omit(eval(parse(text = var_one))),
na.omit(eval(parse(text = var_two)))
)
)
l = l + 1
}
}
FramingTest <- list()
l = 1
for(i in 1:3){
for (j in 1:3) {
var_one <- paste0(
"data$",
infos[i],
conditions[1],
"_",
measures[j]
)
var_two <- paste0(
"data$",
infos[i],
conditions[2],
"_",
measures[j]
)
WilcoxTest <- wilcox.test(
na.omit(eval(parse(text = var_one))),
na.omit(eval(parse(text = var_two))),
paired = FALSE
)
FramingTest[[l]] <- data.table(
var1 = var_one,
var2 = var_two,
w.stat = round(WilcoxTest$statistic, 4),
p.value = round(WilcoxTest$p.value, 4),
cohen.d = cohen.d(
na.omit(eval(parse(text = var_one))),
na.omit(eval(parse(text = var_two)))
)
)
l = l + 1
}
}
FramingTest <- rbindlist(FramingTest)
FramingTest$p.adjust <- p.adjust(FramingTest$p.value)
FramingTest
Effsize <- cohen.d(
na.omit(eval(parse(text = var_one))),
na.omit(eval(parse(text = var_two))
)
FramingTest[[l]] <- data.table(
var1 = var_one,
var2 = var_two,
w.stat = round(WilcoxTest$statistic, 4),
p.value = round(WilcoxTest$p.value, 4),
cohen.d = )
)
Effsize <- cohen.d(
Effsize <- cohen.d(
x
)
)
Effsize <- cohen.d(
Effsize <- cohen.d(
)
)
Effsize <- cohen.d(
)
cohen.d()
cohen.d((eval(parse(text = var_one)), (eval(parse(text = var_two)), na.rm = TRUE)
FramingTest <- list()
l = 1
for(i in 1:3){
for (j in 1:3) {
var_one <- paste0(
"data$",
infos[i],
conditions[1],
"_",
measures[j]
)
var_two <- paste0(
"data$",
infos[i],
conditions[2],
"_",
measures[j]
)
WilcoxTest <- wilcox.test(
na.omit(eval(parse(text = var_one))),
na.omit(eval(parse(text = var_two))),
paired = FALSE
)
Effsize <- cohen.d(
eval(parse(text = var_one)),
eval(parse(text = var_two))
)
FramingTest[[l]] <- data.table(
var1 = var_one,
var2 = var_two,
w.stat = round(WilcoxTest$statistic, 4),
p.value = round(WilcoxTest$p.value, 4),
cohen.d = Effsize$
)
l = l + 1
}
}
for(i in 1:3){
for (j in 1:3) {
var_one <- paste0(
"data$",
infos[i],
conditions[1],
"_",
measures[j]
)
var_two <- paste0(
"data$",
infos[i],
conditions[2],
"_",
measures[j]
)
WilcoxTest <- wilcox.test(
na.omit(eval(parse(text = var_one))),
na.omit(eval(parse(text = var_two))),
paired = FALSE
)
Effsize <- cohen.d(
eval(parse(text = var_one)),
eval(parse(text = var_two))
)
FramingTest[[l]] <- data.table(
var1 = var_one,
var2 = var_two,
w.stat = round(WilcoxTest$statistic, 4),
p.value = round(WilcoxTest$p.value, 4),
cohen.d = Effsize
)
l = l + 1
}
}
View(Effsize)
FramingTest <- list()
l = 1
for(i in 1:3){
for (j in 1:3) {
var_one <- paste0(
"data$",
infos[i],
conditions[1],
"_",
measures[j]
)
var_two <- paste0(
"data$",
infos[i],
conditions[2],
"_",
measures[j]
)
WilcoxTest <- wilcox.test(
na.omit(eval(parse(text = var_one))),
na.omit(eval(parse(text = var_two))),
paired = FALSE
)
Effsize <- cohen.d(
eval(parse(text = var_one)),
eval(parse(text = var_two))
)
FramingTest[[l]] <- data.table(
var1 = var_one,
var2 = var_two,
w.stat = round(WilcoxTest$statistic, 4),
p.value = round(WilcoxTest$p.value, 4),
cohen.d = Effsize$estimate
)
l = l + 1
}
}
FramingTest <- rbindlist(FramingTest)
FramingTest$p.adjust <- p.adjust(FramingTest$p.value)
FramingTest
